{
  "name": "@flytrx/test",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "license": "ISC",
  "author": "Kjell Dießel <development@kettil.de>",
  "keywords": [],
  "engines": {
    "node": ">=16.12",
    "npm": ">=8.1"
  },
  "scripts": {
    "start": "node ./build/index.js",
    "dev": "npx carna start -w",
    "analyse": "npx carna analyse",
    "build": "npx carna build",
    "test": "npx carna test",
    "precarna:build": "rm -rf ./build ./packages/*/build",
    "prepare": "[ \"$CI\" != \"\" ] || ( ls _node_modules/husky > /dev/null 2>&1 && husky install ./node_modules/carna/configs/husky || true )"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@semantic-release/changelog": "6.0.1",
    "@semantic-release/exec": "6.0.2",
    "@semantic-release/git": "10.0.1",
    "@types/node": "16.11.6",
    "carna": "6.2.3",
    "multi-semantic-release": "2.10.0",
    "typescript": "4.4.4"
  }
}
